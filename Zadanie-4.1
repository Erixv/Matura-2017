import java.io.File;
import java.io.FileNotFoundException;

import java.util.Scanner;

public class Zadanie41 {
    public static void main(String[] args) throws FileNotFoundException {
        Scanner scanner = new Scanner(new File("C:\\Users\\erixv\\Documents\\Inf\\punkty.txt"));
        String[] tablica = new String[100];
        int x = 0;
        while (scanner.hasNextLine()) {
            tablica[x] = scanner.nextLine();
            x++;
        }
        int a = 0;
        int b = 0;
        int c = 0;
        int d = 0;
        for (int i = 0; i < tablica.length; i++) {
            String [] Q= tablica[i].split(" ");
            if (Double.valueOf(Q[0])>0&&Double.valueOf(Q[1])>0){
                a++;
            }
            if (Double.valueOf(Q[0])<0&&Double.valueOf(Q[1])>0){
                b++;
            }
            if (Double.valueOf(Q[0])<0&&Double.valueOf(Q[1])<0){
                c++;
            }
            if (Double.valueOf(Q[0])>0&&Double.valueOf(Q[1])<0){
                d++;
            }
        }
        System.out.println(a);
        System.out.println(b);
        System.out.println(c);
        System.out.println(d);
    }
}

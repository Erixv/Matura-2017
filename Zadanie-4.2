import java.io.File;
import java.io.FileNotFoundException;
import java.util.*;

public class Zadanie42 {


    public static void main(String[] args) throws FileNotFoundException {
        Scanner scanner = new Scanner(new File("C:\\Users\\MarMoni\\Documents\\Informatyka\\Okregi.txt"));
        List<String> e= new ArrayList<>();
        while(scanner.hasNextLine()) {
            e.add(scanner.nextLine());
        }
        int ile =0;
        List<String> styczne= new ArrayList<>();
        for (int i = 0; i < e.size(); i++) {
            String[] a =e.get(i).split(" ");

            if(Math.abs(Integer.valueOf(a[1]))==Math.abs(Integer.valueOf(a[2]))) {
                ile++;
                styczne.add(e.get(i));
            }

        }
        Collections.sort(styczne, new comp());
        for (int i = 0; i < styczne.size(); i++) {
            System.out.println(styczne.get(i));
        }
        System.out.println(ile);

    }

}

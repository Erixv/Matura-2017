import java.io.File;
import java.io.FileNotFoundException;
import java.util.Scanner;

public class Zadanie43 {
    public static void main(String[] args) throws FileNotFoundException {
        Scanner scanner = new Scanner(new File("C:\\Users\\erixv\\Documents\\Inf\\punkty.txt"));
        String[] tablica = new String[100];
        int x = 0;
        double P = 0;
        while (scanner.hasNextLine()) {
            tablica[x] = scanner.nextLine();
            x++;
        }
        for (int i = 0; i < tablica.length; i++) {

            if (i == tablica.length) {
                String[] Q = tablica[i].split(" ");
                String[] W = tablica[0].split(" ");
                P = P + ((1 / 2) * (Math.abs((Double.valueOf((Q[0])) * (Double.valueOf(W[1])) - (Double.valueOf(W[0]) * (Double.valueOf(Q[1])))))));
            } else {
                String[] Q = tablica[i].split(" ");
                String[] W = tablica[i++].split(" ");
                P = P + ((1 / 2) * (Math.abs((Double.valueOf((Q[0])) * (Double.valueOf(W[1])) - (Double.valueOf(W[0]) * (Double.valueOf(Q[1])))))));
            }

        }
        System.out.println(P);
    }
}

